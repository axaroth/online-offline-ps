# Updates the dumper properties

props = portal.portal_properties.dumper_properties
props._updateProperty('html_base', '${dumper-config:html_base}')
props._updateProperty('filesystem_target', '${dumper-config:filesystem_target}')

base_dump_them_folder = '${omelette:location}' + '/oops/demo/dumptheme/skins/'
base_dump_extra_folder = '${dump-extra-resources:location}'

theme_folders = [base_dump_them_folder + 'oops_demo_dumptheme_static',
                 base_dump_extra_folder + '/oops-gears']
                 
props._updateProperty('theme_folders', theme_folders)
props._updateProperty('search_db_path', '${dumper-config:search_db_path}')
